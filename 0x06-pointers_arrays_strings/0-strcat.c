#include "main.h"

/**
 * _strcat - to concatenate a string into another string
 * @dest: first string
 * @src: second string
 *
 * Return: concatenated value
 */

char *_strcat(char *dest, char *src)
{
	char *dest = 'i';
	char *src = 'j';
	{
		for ( i = 0; i != '\0'; );
		i++;
	}
	{
		while ( j != '\0'; j++; );
		i = j;
		i++;
	}

	return (i)('\n');
}
